from setuptools import setup, find_packages

setup(
    name='hallmonitor',
    version='2.0',
    packages=find_packages(),
    install_requires=[
        'pytest',  # or other dependencies
    ],
)

